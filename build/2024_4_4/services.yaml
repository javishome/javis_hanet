write_person:
  name: Log person
  description: Log person
  fields:
    payload:
      name: Payload
      description: Payload
      required: true
      selector:
        text:

push_to_qcd:
  name: push all data to qcd
  description: push all data to qcd
  fields:
    secret_key:
      name: Secret key
      description:  Secret key
      required: true
      selector:
        text:

update_period:
  name: Update period
  description: Update period
  fields:
    person_id:
      name: Person ID
      description: Person ID
      required: true
      selector:
        text:
    start_time:
      name: Start time
      description: Start time
      required: true
      selector:
        date:
    end_time:
      name: End time
      description: End time
      required: true
      selector:
        date:

delete_period:
  name: Delete period
  description: Delete period
  fields:
    person_id:
      name: Person ID
      description: Person ID
      required: true
      selector:
        text:
check_faceid_group_sensor:
  name: Check FaceID Group Sensor
  description: Check FaceID Group Sensor